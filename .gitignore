# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local process notes (outreach, submission tracking) — never committed
process/

# arXiv submission packages
*_arxiv.tar.gz

# Lean 4 formalization: track only what accompanies the papers publicly —
# source, toolchain/manifest pins, LICENSE, README. Internal working ledgers
# stay local, as do the build dir (~7.5 GB; also excluded by lean/.gitignore)
# and session/CI scaffolding.
lean/.lake/
lean/.github/
lean/AXIOMS.md
lean/GAPS.md
lean/STATUS.md

# LaTeX build artifacts (committed PDFs are intentional)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
